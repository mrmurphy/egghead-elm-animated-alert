# Egghead.io - Elm Animated Alert

## Quickstart

 - `npm install`
 - `npm start` : Watches the filesystem, and recompiles when Elm or CSS files have changed. Also serves up the content with browser-sync for live-reloading.
